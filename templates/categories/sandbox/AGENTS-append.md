<!-- ===== append：sandbox 分类纪律（拼接到 AGENTS-core 之后，3 条） ===== -->

## 沙盒纪律

1. **master 直提，无 CI 无发布**：不打 tag、不发 npm/PyPI；验证靠本地跑通（提交规范与机密红线仍适用）。
2. **收尾闭环**：实验结束必须在 README「结论」节写验证结果。
3. **转正路径**：需要发布/协作时走 `scaffold.mjs` 目标分类全量脚手架迁移，**不在本仓打补丁凑规范**。
