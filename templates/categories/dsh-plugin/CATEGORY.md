<!-- 模板说明（用后删除）：A 类 dsh 插件单库（默认分类）。
     即 `templates/repo-root/` 基类本体——无 delta；分类表见 REPO-CLASSIFICATION.md。 -->

# A 类 · dsh 插件单库（基类）

`scaffold.mjs --type dsh-plugin`（默认）= 直接使用 `templates/repo-root/`。
适用：dsh 生态 6 个插件单库（context-compass / subagent-router / imgdraw / knowledge-sqlite /
session-slm-router / subagent-cursor）。红线：部署纪律四件套（AGENTS 5 条 + DEVELOPMENT +
pre-commit + check-deploy）。
