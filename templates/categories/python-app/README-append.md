<!-- ===== append：python-app 分类段（拼接到 README-core 之后） ===== -->

[![PyPI](https://img.shields.io/pypi/v/<name>.svg)](https://pypi.org/project/<name>/)



- <功能点 1：一句话说清用户得到什么>
- <功能点 2>

## 安装

> 要求 Python >= 3.11（见 pyproject.toml requires-python）。

```sh
pip install <name>
# 开发：python -m venv .venv && . .venv/bin/activate && pip install -e ".[dev]"
```

## 使用

<最小可复制示例：CLI 命令 / import 调用。示例代码块必须可直接粘贴执行。>

## 配置

| 配置项 | 默认 | 说明 |
|---|---|---|
| `<field>` | `<default>` | <作用与生效方式（live / 重启生效）> |
