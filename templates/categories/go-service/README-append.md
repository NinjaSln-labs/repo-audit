<!-- ===== append：go-service 分类段（拼接到 README-core 之后） ===== -->



- <功能点 1：一句话说清用户得到什么>
- <功能点 2>

## 安装

> 要求 Go >= <版本>（见 go.mod）；或直接下载 Release 二进制。

```sh
go install github.com/<org>/<repo>/cmd/<name>@latest
# 或从 GitHub Release 下载对应平台归档（含 checksums.txt 校验）
# 服务部署：deploy/ 下模板 + docs/ 说明
```

## 使用

<最小可复制示例：启动命令 / 配置文件最小样例。示例代码块必须可直接粘贴执行。>

## 配置

| 配置项 | 默认 | 说明 |
|---|---|---|
| `<field>` | `<default>` | <作用与生效方式（重启生效 /热更新）> |
